import S3SyncClient from 's3-sync-client'
import { S3Client } from '@aws-sdk/client-s3'

// Exporting and Importing in App.js
// just to run the line no. 1 and 2
export const TestVariable = 'TestVariable'
